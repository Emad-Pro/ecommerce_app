name: ecommerce_app
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  intl_phone_field: ^3.2.0
  pretty_dio_logger: ^1.4.0
  auto_direction: 
  internet_connection_checker: ^3.0.1
  
  # تم نقل كل الحزم الأساسية إلى هنا
  flutter_bloc:
  dio:
  shared_preferences:
  get_it:
  cached_network_image:
  shimmer:
  bloc:
  equatable:
  dartz: 

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true


  fonts:
    - family: cairo
      fonts:
        - asset: assets/font/cairo/cairo.ttf